<nav>
  <%= link_to t('views.navigation.go_back'), manufacturers_path, class: 'btn btn-secondary' %>
</nav>

<main class='row'>
  <div class='col-md-12'>
    <header class='header'>
      <h1><%= t('views.actions.update') %> <%= t('activerecord.models.manufacturer.one') %></h1>
    </header>

    <section class='contents'>
      <%= render 'form' %>
    </section>

  </div>
</main>
