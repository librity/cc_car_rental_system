<p><%= t('views.devise.mailer.unlock_instructions.greeting', email: @resource.email) %></p>

<p><%= t('views.devise.mailer.password_change.message1') %></p>

<p><%= t('views.devise.mailer.password_change.message2') %></p>

<p><%= link_to t('views.devise.mailer.unlock_instructions.unlock_account'), unlock_url(@resource, unlock_token: @token) %></p>
