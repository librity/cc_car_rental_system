<nav>
  <%= link_to t('views.actions.go_back'), subsidiaries_path, class: 'btn btn-secondary' %>
</nav>

<main class='row'>
  <div class='col-md-12'>
    <header class='header'>
      <h1><%= t('views.actions.update') %> <%= t('views.resources.subsidiaries.singular') %></h1>
    </header>

    <section class='contents'>
      <%= render 'form' %>
    </section>

  </div>
</main>
