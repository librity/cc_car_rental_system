---
pt-BR:
  models:
    validations:
      not_empty: "%{attribute} não pode ficar em branco"
      unique: "%{attribute} deve ser único"

    manufacturer:
      validations:

    car_category:
      validations:

    car_model:
      validations:

    subsidiary:
      validations:

    customer:
      validations:
